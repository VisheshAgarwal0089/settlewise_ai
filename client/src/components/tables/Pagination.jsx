export function Pagination({ meta, onPage }) { if (!meta || meta.totalPages <= 1) return null; return <div className="pagination"><span>Page {meta.page} of {meta.totalPages} · {meta.total} records</span><div><button className="secondary" disabled={meta.page <= 1} onClick={() => onPage(meta.page - 1)}>Previous</button><button className="secondary" disabled={meta.page >= meta.totalPages} onClick={() => onPage(meta.page + 1)}>Next</button></div></div>; }
